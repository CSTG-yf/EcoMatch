# 银行问数系统前端重构说明

## 1. 改造定位

本次改造将通用数据分析前端收敛为银行业智能问数系统。系统以唯一的「银行问数」Agent 作为问数基座，负责自然语言理解、指标识别、口径匹配、银行 NL2SQL 查询计划和结果回答。

场景只是用户使用问数能力的场景归纳，不作为指标分类依据。指标、指标口径、数据模型、数据集和数据源属于数据治理与管理配置范围。

## 2. 路由与角色

原始路由保持注册和可访问路径不变，原始版本与当前版本均为 43 条路由，没有新增或删除业务路由。

用户侧保留：

- 问数对话
- 分析看板
- 指标市场
- 导出中心

管理员侧通过「管理中心」进入管理区域，管理页面在侧边栏展示二级目录，不再使用多级级联选择。管理区域包含问数配置、数据模型、指标与口径治理、数据源、大模型连接、系统管理、评估和安全运营等原有功能入口。

首页由原始的 `/model` 改为 `/chat`，使登录后的默认工作流直接进入银行问数。

## 3. 唯一 Agent 设计

前端不再提供新建或删除助理入口，只保留「银行问数」Agent 的配置编辑。底层 Agent API 和兼容能力仍保留，避免影响历史会话、Copilot、评估和既有接口。

用户登录后普通入口默认选择「银行问数」，不会因为浏览器中遗留的 Agent 选择把用户带回闲聊或样例 Agent。

## 4. 模型选择与配置

比赛要求的模型选择和模型配置能力由现有系统提供，当前链路保持可用：

1. 管理员在管理中心的大模型连接页面创建、编辑、删除和测试模型连接。
2. Agent 编辑页面的大模型配置页读取可用模型连接列表。
3. 每个问数应用场景可以选择对应的 `chatModelId`，包括 S2SQL 解析、执行修复、最终回答和多轮改写等阶段。
4. 保存 Agent 后，后端按 Agent 的 `chatAppConfig` 读取对应模型配置并执行。
5. 启动时不绑定聊天模型；如果管理员没有完成模型选择，Agent 保持待配置状态。管理员选择模型后，银行专用查询计划阶段可以使用该配置，避免把模型连接管理和启动初始化耦合。

本次验收只确认项目内部的模型列表、模型参数、模型场景配置、管理员保存和 Agent 模型绑定接口链路，不验收外部推理服务的启动、网络连通性或模型实际响应。外部推理服务由部署环境提供，管理员在项目启动后录入配置。

## 5. 权限边界

数据权限不应挂在唯一 Agent 的数据库配置上。Agent 是统一的业务能力基座，不应该因为不同用户登录而被重新绑定到不同数据库，否则会导致并发请求之间的配置覆盖和数据越权风险。

后续应建设独立的数据权限管理能力，按照用户、角色、组织、数据源、语义模型、机构范围和指标范围控制访问，并由服务端在查询执行前注入和校验行级权限。细粒度权限管理由独立工作继续完成，本次前端路由重构不把它混入 Agent 配置。

## 6. 原始功能保留

已保留原始问答、会话历史、看板、看板编辑与查看、导出中心、指标市场、指标详情、数据模型、数据集、维度、数据治理、标签、数据库、大模型连接、插件、系统、评估、安全运营、分享、移动端和外部对话入口。

有意收口的只有多助理初始化、新建助理和删除助理入口，原因是比赛要求专用银行问数 Agent 系统，而不是面向用户的多 Agent 平台。

## 7. 后续前端计划

后续工作在当前路由框架上逐页细化，不改变现有业务边界：

1. 优化问数工作台的信息层级、提问状态、结果表格、图表和回答证据展示。
2. 优化分析看板的编辑、查看、筛选、分享和空状态体验。
3. 优化指标市场、指标详情和口径治理页面，突出指标定义、计算公式、来源和适用范围。
4. 优化数据模型、数据集、维度和数据源页面，统一表格、筛选、详情和编辑体验。
5. 优化大模型连接与问数配置页面，明确模型连接、应用阶段、提示词和保存状态的关系。
6. 完成独立数据权限管理页面，与 Agent 配置页面分离。
7. 优化系统管理、评估、安全运营和导出中心页面的状态反馈、错误处理和视觉一致性。
8. 对桌面端、移动端、加载、空数据、无权限、失败和超时状态逐页补齐验收。

## 8. 本次变更验证

- 原始路由数量：43
- 当前路由数量：43
- 新增业务路由：0
- 删除业务路由：0
- 前端登录和问数链路：已实际验证
- 启动配置专项测试：13/13 通过
- Python 全量测试：364 个测试通过
- 启动时外部推理服务配置：不自动读取、不自动绑定
- 外部推理服务启动与连通性：不属于本次验收范围
